
import serial
import time
import datetime
import os
import csv
import numpy as np 
import re
import matplotlib as mpl
import matplotlib.pyplot as plt
from matplotlib.dates import DateFormatter

def dec10(data, time):
    DecimateData = np.zeros(( data.shape[0]//10, 13 ))
    DecimateTime = [None]*(data.shape[0]//10)
    for (i,j) in enumerate(range(0,len(DecimateTime)//10*10,10)):
      #by default, sum acts on first dimension only
      DecimateData[i,:] = sum(data[j:(j+10),:])/10
      DecimateTime[i] = time[j+5]
    decimated = np.append( decimated,
                           DecimateData,
                           axis=0)
    t_decimated.extend(DecimateTime)
    data = np.zeros((1,13))
    time = [ datetime.datetime.now() ]
